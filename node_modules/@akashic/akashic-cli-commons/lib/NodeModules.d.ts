import { Logger } from "./Logger";
import { ModuleMainScripts } from "./index";
export declare module NodeModules {
    function listModuleFiles(basepath: string, modules: string | string[], logger?: Logger): Promise<string[]>;
    function listPackageJsonsFromScriptsPath(basepath: string, filepaths: string[]): string[];
    function listModuleMainScripts(packageJsonFiles: string[]): ModuleMainScripts;
    function listScriptFiles(basepath: string, modules: string | string[], logger: Logger): Promise<string[]>;
}
