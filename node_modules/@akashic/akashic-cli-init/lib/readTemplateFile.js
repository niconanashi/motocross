"use strict";
var fs = require("fs");
var path = require("path");
function readTemplateFile(param) {
    var copySpecPath = path.join(param._realTemplateDirectory, param.type, "template.json");
    return new Promise(function (resolve, reject) {
        fs.readFile(copySpecPath, function (err, data) {
            if (err) {
                if (err.code !== "ENOENT") {
                    console.log("reg", err);
                    reject(err);
                    return;
                }
                resolve({});
                return;
            }
            var templateConfig = JSON.parse(data);
            resolve(templateConfig);
        });
    });
}
exports.readTemplateFile = readTemplateFile;
//# sourceMappingURL=readTemplateFile.js.map