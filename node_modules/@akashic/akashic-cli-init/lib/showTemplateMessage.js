"use strict";
/**
 * template.json の guideMessage 要素を出力する
 */
function showTemplateMessage(templateConfig, param) {
    return showTemplateGuideMessage(templateConfig, param);
}
exports.showTemplateMessage = showTemplateMessage;
function showTemplateGuideMessage(templateConfig, param) {
    var guideMessage = templateConfig.guideMessage;
    if (!guideMessage)
        return Promise.resolve();
    var texts = guideMessage.split("\n");
    texts.forEach(function (text) {
        param.logger.print(text);
    });
    Promise.resolve();
}
//# sourceMappingURL=showTemplateMessage.js.map