import { Logger, GameConfiguration } from "@akashic/akashic-cli-commons";
export interface StatSizeParameterObject {
    logger: Logger;
    basepath: string;
    game: GameConfiguration;
    limit?: string;
    raw: boolean;
}
export declare function size(param: StatSizeParameterObject): Promise<void>;
