"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var https = require("https");
function getFromHttps(url) {
    return new Promise(function (resolve, reject) {
        var request = https.get(url, function (res) {
            if (res.statusCode >= 400) {
                return reject(new Error("Failed to get resource. url: " + url + ". status code: " + res.statusCode + "."));
            }
            var bodies = [];
            res.on("data", function (chunk) {
                bodies.push(chunk.toString());
            });
            res.on("end", function () { return resolve(bodies.join("")); });
        });
        request.on("error", function (err) { return reject(err); });
    });
}
exports.getFromHttps = getFromHttps;
