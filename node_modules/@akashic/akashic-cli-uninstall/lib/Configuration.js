"use strict";
var __extends = (this && this.__extends) || (function () {
    var extendStatics = Object.setPrototypeOf ||
        ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
        function (d, b) { for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p]; };
    return function (d, b) {
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
var cmn = require("@akashic/akashic-cli-commons");
var Configuration = /** @class */ (function (_super) {
    __extends(Configuration, _super);
    function Configuration() {
        return _super !== null && _super.apply(this, arguments) || this;
    }
    Configuration.prototype.removeOperationPlugin = function (moduleName) {
        if (!this._content.operationPlugins)
            this._content.operationPlugins = [];
        var moduleNameNoVer = cmn.Util.makeModuleNameNoVer(moduleName);
        var operationPlugins = this._content.operationPlugins;
        operationPlugins = operationPlugins.filter(function (e) { return e.script !== moduleNameNoVer; });
        if (operationPlugins.length === this._content.operationPlugins.length)
            this._logger.info("No entry for " + moduleName + " in operationPlugins.");
        this._content.operationPlugins = operationPlugins;
    };
    return Configuration;
}(cmn.Configuration));
exports.Configuration = Configuration;
